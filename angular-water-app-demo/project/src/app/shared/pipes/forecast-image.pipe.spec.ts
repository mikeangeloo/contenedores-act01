import { ForecastImagePipe } from './forecast-image.pipe';

describe('ForecastImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ForecastImagePipe();
    expect(pipe).toBeTruthy();
  });
});
